# Minimal config to be somewhat usable

alias e="nvim -u NORC"
alias g=git
alias ls="ls --group-directories-first -F --color=auto"

EDITOR=vim

# Minimal prompt (with yellow cwd)
PS1="\[\e[33m\]\w\[\e[0m\] > "

# vim:set ft=bash:
