# Config for plugin `git-trim` to help delete local merged branches
#   https://github.com/foriequal0/git-trim

[trim]
  # Branches considered as base branches
  bases = develop,dev,main,master

[alias]
  trim = trim
  trim-stray = trim --delete=stray

# vim:set ft=gitconfig:
