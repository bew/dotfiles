---
description: Produce a handoff document for this session. Pass any context for the next session (focus, output dir, …).
subtask: false # shared context!
---

FIRST: Load the `handoff` skill and follow its instructions.

## User context

May be empty. If non-empty, treat as input to the skill's Setup step —
may contain focus, output directory, or other relevant context.

```
$ARGUMENTS
```
