# Phase:Wrap — freeze settled topics; exit

Confirm every topic's final status (`active`/`frozen`/`revived`) is recorded
per the mode in <../artefacts.md>.
Freeze any topic the user declares settled; note genuinely unresolved `Open threads`.

Summarise what settled, what stays open, and where the file(s) live.

Ready to exit? (the exploration files remain for a later spec or a re-entrant session)