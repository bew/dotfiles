#!/usr/bin/env bash
# Touch all given paths, creating parent directories as needed.

set -euo pipefail # Safe, strict script execution

for f in "$@"; do
  dir=$(dirname "$f")
  if [[ -n "$dir" ]] && ! [[ -d "$dir" ]]; then
    mkdir -vp "$dir"
  fi
  touch $f
  echo "touch-all: touched '$f'"
done
